# fixtures

M0 验证时保存的微博原始响应样本（见 `../docs/m0-verification-checklist.md`）。

- 仅本地使用：可能包含其他用户的评论内容与你的账号相关数据，不要公开、不要提交到公开仓库。
- `live/` 为 2026-09-13 自动验证抓取的响应；根目录的样本来自浏览器手动抓取。
- 验证用的 Cookie 已移至项目根目录 `cookie.txt`，请勿放进本目录或提交。
- 这些样本后续作为过滤引擎单测与接口回归的输入。
